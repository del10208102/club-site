# 管理接口密钥：设置后，发布公告 / 上传作品 / 修改招新说明均需携带此密钥
# （表单字段 admin_token 或请求头 x-admin-secret）
ADMIN_SECRET=请改为长随机字符串

# 可选：JSON 数据目录，默认 ./data（内含 site.json）
# DATA_DIR=data

# 可选：上传文件目录，默认 ./uploads
# UPLOAD_DIR=uploads

# Node 生产环境
NODE_ENV=production
